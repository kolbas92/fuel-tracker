# Добавь это в свой Caddyfile на сервере (рядом с gig36 блоками)

fuel-api.твойдомен.ru {
    reverse_proxy localhost:8000
}

# Фронт деплоится на Vercel — домен настраивается там
# Если хочешь раздавать фронт тоже с VPS:
# fuel.твойдомен.ru {
#     root * /var/www/fuel-tracker
#     file_server
# }
